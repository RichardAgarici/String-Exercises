 System.out.println(str1.startsWith("Shah",4));
 = boolean care verifica daca stringul care incepe la index 4(3) este egal cu sintaxa "Shah"

     System.out.println(str2.indexOf('.',4));
     =returneaza un intiger care incepe sa verifice indexul unde gaseste . incepand de la index 4 al stringului

     System.out.println(str2.lastIndexOf('.'));
     =returneaza un int calculand invers de la capat la coada indexul stringului


        String str3="python";
        String str4="python";

        str3.equals(str4);
        =compara OBIECTUL str3 cu str4 si returneaza un boolean bazat pe asta.

        str1.equalsIgnoreCase(str2)
        =compara daca 2 stringuri sunt egale lexial (java=JAVA). Return boolean.

        .concat() cu + e tot una

        String str3="sss";
        System.out.println(str3.matches("."));
        aici matches este un boolean care verifica daca stringul contine numarul de caractere ca si in regexul atasat. adica 1
        cum sss is 3 litere, va returna mereu false


         String str4="p";
         System.out.println(str4.matches("[^abc]"));
         returneaza un boolean daca NU gaseste abc

         String str5="b";
         System.out.println(str5.matches("[abc]"));
         returneaza un boolean daca gaseste DOAR 1 din caracterele mentionate in paranteza

